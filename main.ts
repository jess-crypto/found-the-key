print('pinNumber is: ' + pinNumber
);
print(foundKey === "yes", "&&" 'pinNumber' === 10),
    print('You opened the lock!');
if(foundKey === 'no' && pinNumber !==10){
    print('The pin number is incorrect.');
}